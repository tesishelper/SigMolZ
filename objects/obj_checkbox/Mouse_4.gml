
if (mouse_x > x_pos && mouse_x < x_pos + width &&
    mouse_y > y_pos && mouse_y < y_pos + height) {
    obj_game.dinamic_model = true; //!obj_game.dinamic_model; // Cambia de estado
    obj_game.static_model = false; //!obj_game.static_model;
   
}